import draggable from "./draggable";
import type { DraggableOptions, DragData } from "./type";
import { makeDraggable, DRAGGABLE_FLAG, type DragContext } from "./core";

export { makeDraggable, DRAGGABLE_FLAG };
export { DraggableOptions, DragData, DragContext };

export default draggable;
